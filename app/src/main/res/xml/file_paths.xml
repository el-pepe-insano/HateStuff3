<?xml version="1.0" encoding="utf-8"?>
<paths>

    <!-- Cache interno (recomendado para cámara) -->
    <cache-path
        name="cache"
        path="." />

    <!-- Files interno de la app -->
    <files-path
        name="files"
        path="." />

</paths>
